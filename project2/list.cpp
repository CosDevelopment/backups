#include "volsort.h"

////Not needed because used a header initiliazation of the size which is okay as of c++11 standards for in class definitions;
///all my pointers are smart pointers so they delete themselves so I dont need a deconstructor to delete the lists
///so no need for definitions, just need to keep the defaults